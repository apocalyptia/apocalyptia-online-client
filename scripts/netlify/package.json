{
  "scripts": {
    "build": "npm run build:app && npm run build:netlify",
    "build:app": "cd ../.. && npm i && npm audit fix && npm run build",
    "build:netlify": "node build && cd api/ssr && npm i && npm audit fix",
    "deploy": "npm run build:netlify && netlify deploy"
  },
  "dependencies": {
    "fs-extra": "^8.1.0"
  }
}