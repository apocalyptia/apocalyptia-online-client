// export default (ctx, x, y, r, Rw, h, fill) => {
// const fill = fill || false
// ctx.beginPath()
// ctx.moveTo(x + r, y)
// ctx.lineTo(x + Rw, y + h)
// ctx.lineTo(x + Rw, y + h + l)
// ctx.lineTo(x + r, y + Rh)
// ctx.lineTo(x, y + l + h)
// ctx.lineTo(x, y + h)
// ctx.closePath()
// if (fill) ctx.fill()
// else ctx.stroke()
// }
