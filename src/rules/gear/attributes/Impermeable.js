import Attribute from '$classes/gear/Attribute.js'

const Impermeable = new Attribute({
	name: `Impermeable`,
	description: [`Automatic Success to resist exposure to Diseases and Toxins.`],
})

export default Impermeable
