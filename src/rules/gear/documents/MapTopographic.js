import Document from '/src/classes/gear/Document.js'

const MapTopographic = new Document({
	name: `Map (Topographic)`,
	description: [`+3 Survival(Navigate) in a given Region.`],
	size: 0,
	type: `Document`,
})

export default MapTopographic
