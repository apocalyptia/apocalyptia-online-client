import Attribute from '$classes/gear/Attribute.js'

const Scatter = new Attribute({
	name: `Scatter`,
	description: [`Range penalties apply to Damage instead of Attack.`, `-1 Penetration.`],
})

export default Scatter
