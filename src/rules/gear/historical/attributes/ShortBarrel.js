import Attribute from '/src/classes/gear/Attribute.js'

const ShortBarrel = new Attribute({
	name: `Short-Barrel`,
	description: [`[ Range / 2 ] and [ Size / 2 ] (Round up).`],
})

export default ShortBarrel
