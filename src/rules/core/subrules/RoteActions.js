import Core from '/src/classes/Core.js'

const RoteActions = new Core({
	name: `Rote Actions`,
	description: [`If the roll is unopposed and your [ ( score + Modifiers ) >= # ] before the roll and you can take your time, you Succeed automatically.`],
})

export default RoteActions
